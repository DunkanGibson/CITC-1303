1. select INITCAP(city), UPPER(state)
	from zipcode
	
2. select LTRIM(RTRIM('0008954210000', '0'), '0') both
	from DUAL
	
3. select SUBSTR(first_name, 1, 4)
	from instructor
	
4. SELECT first_name, last_name
	FROM student
		WHERE first_name LIKE 'Z%'
			ORDER BY LENGTH(last_name)
			
5. SELECT FLOOR(19/7)
     FROM DUAL

6. SELECT last_name
	FROM instructor
		WHERE LENGTH(last_name) <= 7
		
7. SELECT last_name || ',' || ' ' || first_name
	FROM student
	
8. SELECT REPLACE('Today is Monday','Monday','Tuesday')
	FROM DUAL
	
9. The DUAL table is a table that holds no signifigant data of its own. It can be used in conjuction with functions to select values that do not exist in tables. It does not belong to the STUDENT schema it belongs to the user SYS.

10. A NVL function can be used to avoid such a case.
